const btn=document.querySelector('.menu-btn');const links=document.querySelector('.nav-links');if(btn&&links){btn.addEventListener('click',()=>{const open=links.classList.toggle('open');btn.setAttribute('aria-expanded',open?'true':'false')});links.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{links.classList.remove('open');btn.setAttribute('aria-expanded','false')}));}
document.querySelectorAll('[data-year]').forEach(el=>el.textContent=new Date().getFullYear());
// Conversion hooks: work with Plausible or Google Analytics if one is enabled later; otherwise no-op.
window.endursysTrack=window.endursysTrack||function(event,props){try{if(typeof window.plausible==='function')window.plausible(event,{props:props||{}});if(typeof window.gtag==='function')window.gtag('event',event,props||{});}catch(e){}};
document.addEventListener('click',e=>{const a=e.target.closest('a');if(!a)return;const href=a.getAttribute('href')||'';let event=a.dataset.track||'';if(!event&&href==='contact.html')event='contact_click';if(!event&&href.includes('cas.html'))event='case_studies_click';if(!event&&href.startsWith('mailto:'))event='email_click';if(event)window.endursysTrack(event,{label:(a.textContent||'').trim().slice(0,80),href});});
document.querySelectorAll('form[data-track-form]').forEach(f=>f.addEventListener('submit',()=>window.endursysTrack(f.dataset.trackForm||'form_submit',{page:location.pathname})));
